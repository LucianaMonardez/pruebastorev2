import { CountItemsPipe } from './count-items.pipe';

describe('CountItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new CountItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
